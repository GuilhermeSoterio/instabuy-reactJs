import { initializeApp } from 'firebase/app';

// Inicializa o Firebase globalmente para testes
initializeApp({
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_AUTH_DOMAIN',
  projectId: 'YOUR_PROJECT_ID',
});